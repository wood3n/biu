import React from 'react';
import PageContainer from '@/components/PageContainer';

interface Props {
}

/**
 * 私人 FM
 */
const FM: React.FC<Props> = (props) => {
  return (
    <PageContainer>
      <div>私人FM</div>
    </PageContainer>
  );
};

export default FM;
